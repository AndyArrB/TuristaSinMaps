<div class="card text-white bg-{{ $bgColor }} mb-3">
    <div class="card-header">{{ $header }}</div>
    <div class="card-body">
        <h5 class="card-title">{{ $title }}</h5>
        <p class="card-text">{{ $text }}</p>
        <a href="{{ $btnLink }}" class="btn btn-light">{{ $btnText }}</a>
    </div>
</div>
